import { makeStyles } from "@mui/material/styles";

export const useStyles = makeStyles(() => {
  return {
    iconBlock: {
      paddingRight: "35px",
      paddingTop: "10px",
    },
    themeIcon: {
      marginRight: "45px",
    },
  };
});
